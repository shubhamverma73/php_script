<?php
// In PHP, the define() function is used to create a constant, which is a named storage that holds a fixed value. Constants are like variables but their values cannot be changed after they are set, and they are globally accessible, meaning they can be used from anywhere in your script.

define('APP_NAME', 'ABC');
define('DB_NAME', 'MY_DB');
define('DB_USERNAME', 'DB_USER');
define('DB_PASS', 'JFKJD5454&*(&FK');
define('DB_HOST', 'localhost');